<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game.cowsbulls">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".scenes.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".scenes.host.HostActivity">
            <intent-filter>
                <action android:name="android.intent.action.HOST" />
            </intent-filter>
        </activity>

        <activity android:name=".scenes.join.JoinActivity">
            <intent-filter>
                <action android:name="android.intent.action.JOIN" />
            </intent-filter>
        </activity>

        <activity android:name=".scenes.gamesetup.GameSetupActivity">
            <intent-filter>
                <action android:name="android.intent.action.GAMESETUP" />
            </intent-filter>
        </activity>

        <activity android:name=".scenes.gamesession.GameSessionActivity">
            <intent-filter>
                <action android:name="android.intent.action.GAMESESSION" />
            </intent-filter>
        </activity>

        <activity android:name=".scenes.gameplay.GameplayActivity">
            <intent-filter>
                <action android:name="android.intent.action.GAMEPLAY" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
</manifest>